# SvelteKit Environment Variables

# Port для production сервера (по умолчанию 3001)
PORT=3001

# Node Environment
NODE_ENV=production

# Host (для Docker)
HOST=0.0.0.0

# Public variables (доступны в браузере, префикс PUBLIC_)
# PUBLIC_API_URL=http://localhost:4000
